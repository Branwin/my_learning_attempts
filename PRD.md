# 产品需求文档（PRD）  
项目：学习辅助知识点收藏 APP（Android / Flutter）  
版本：v1（MVP）  
日期：2026-09-09  
负责人：Branwin

---

## 1. 产品目标和使用场景
目标：
- 为学生/自学者提供一个便捷的移动端工具，用于搜集、整理、复习与记录知识点，帮助建立持续学习习惯并管理每日学习计划。
- 首版在 Android 平台上以 Flutter 实现，后端使用 Firebase（Authentication、Firestore、Cloud Storage），支持离线编辑并在联网时同步云端。

主要使用场景：
- 学生导入/识别课表后，将课程与知识点关联，按课程组织学习内容。
- 在课堂或自习时快速收藏知识点并为其添加笔记（文本、图片、手写、附件）。
- 每天查看首页推荐与“今日计划”，执行与调整学习任务。
- 用户在离线时也可编辑卡片/笔记，网络恢复后自动同步，并在冲突时保留双方供用户合并或选择。
- 用户能够更换主题配色，个性化界面。

关键约束：
- 平台：Android（首版）
- 前端：Flutter
- 后端：Firebase（Auth + Firestore + Cloud Storage）
- OCR：图片默认上传到云端进行识别（导入页需提示并要求用户确认）
- 单设备登录（不允许同时在多设备登录同一账号）；必须支持强制登出其它设备

---

## 2. 整体页面框架与导航结构
底部主导航（四 Tab）：
- 首页（Home） —— 搜索、每日推荐、今日计划
- 课程（Course / Timetable / Import） —— 课表管理与导入（OCR）
- 收藏/集合（Collections） —— 知识点集合/专题板与批量管理
- 我（Profile / Settings） —— 账号、同步、主题、隐私设置

次级/模块页面（示例）：
- 登录/注册/找回密码
- 知识卡片详情、创建/编辑页
- 笔记编辑器（卡片内）
- 课表导入 — OCR 上传与预览页
- 每日计划编辑页（计划列表、拖拽排序）
- 冲突合并弹窗 / 冲突中心
- 设置页（主题、隐私、同步、删除账号）

---

## 3. 首页的内容结构
页面从上到下的主要区块：
1. 顶部搜索栏
   - 占位文案：搜索知识点标题，按回车查看更多
   - 支持模糊匹配与自动补全建议（基于标题）
2. 每日推荐区
   - 随机产生（每次打开 APP 随机刷新），支持手动刷新和无限滚动加载
   - 卡片模式（Knowledge Card）：标题、课程标签、重要性徽章、摘要、操作（收藏/加入计划/分享/更多）
   - 推荐卡片可固定（用户手动操作）
3. 今日计划区
   - 列表展示今日计划项（可拖拽排序）
   - 每项显示预计时长/所属课程/完成复选框/拖拽手柄
   - 支持启用/禁用、标记已完成、延后（Snooze）
4. 底部导航
   - 导航到课程、收藏/集合、我

交互要点：
- 搜索栏优先搜索标题（v1）；点击搜索结果进入卡片详情
- 推荐为纯随机，避免复杂推荐规则（v1）
- 每日计划项可直接从卡片加入并设置预计时长

---

## 4. 每个模块的用途（简要）
- 首页（Home）：发现（每日推荐）、检索（搜索栏）、执行（今日计划）。
- 课程（Course / Timetable）：管理学期与课表；手动添加课程；通过 OCR 导入课表；课程与知识点建立关联。
- 收藏/集合（Collections）：按集合/专题组织知识点，进行批量管理（移动/删除/导出）。
- 知识卡片（Card）：存放单个知识点的结构化信息与关联笔记，作为学习单元。
- 笔记（Note）：附属于知识卡片的用户笔记（多条），支持文本、图片、手写、附件。
- 每日计划（Plan）：用户手动创建的当天学习任务、时长与顺序。
- OCR 导入模块：将课表图片识别为课程条目并映射到课表结构（预览/校正后导入）。
- 冲突管理（Conflict）：处理离线/多端同步冲突，保留双方并提示用户选择或合并。
- 我/设置（Profile）：账号管理、同步/隐私设置、主题、删除账号等。

---

## 5. 每个模块第一版最必要的数据和操作
说明：列出模块在 v1（必须完成）中的最小字段与用户操作。

1) 账号（User）
   - 必要数据字段：userId、用户名、邮箱、头像URL、sessionToken
   - 操作：注册（邮箱+密码）、手机号登录（验证码）、邮箱验证、重置密码、编辑资料、删除账号、强制登出其它设备

2) 知识卡片（Card）
   - 必要数据字段：
     - cardId、title（标题）*、description（简要描述/定义）*、courseId（所属课程，可空）、tags（数组）、priority（重要性/优先级）、source（来源/引用URL或文本）、resources（附件引用列表）、collections（所属集合列表）、createdAt、updatedAt、lastReviewAt（可选）
   - 操作：创建/编辑/删除/复制/分享、收藏/取消收藏、加入/移出集合、关联课程

3) 笔记（Note）
   - 必要数据字段：noteId、cardId、authorId、content（纯文本）、attachments（图片/文件引用）、createdAt、updatedAt
   - 操作：添加笔记（在卡片详情页）、编辑笔记、删除笔记、上传附件（图片/PDF/文件）、手写拍照作为图片上传

4) 课程与课表（Course / Timetable）
   - 必要数据字段：
     - courseId、name（课程名）*、teacher、location、weeks（周次范围，例如 1-16）、weekday（星期几）、startSection/endSection 或 startTime/endTime、term（学期）、notes
   - 操作：手动添加/编辑/删除课程、导入（OCR）、切换学期、将课程作为集合创建入口、合并/覆盖冲突询问

5) 集合/专题（Collection）
   - 必要数据字段：collectionId、name、description、cardIds（列表）、createdAt
   - 操作：新建集合、将卡片加入/移出集合、批量移动/删除/导出

6) 每日计划（Plan / PlanItem）
   - 必要数据字段：planId（每日计划容器）、items（数组：itemId、cardId、scheduledTime（可选）、estimatedDuration、status（pending/done/skipped））
   - 操作：创建计划项（选择知识点并可设预计时长）、拖拽重排、标记完成/延后/删除

7) OCR 导入（Import）
   - 必要数据��段：uploadId、uploaderId、images（列表，存储在 Cloud Storage）、recognitionResult（临时表格结构）、confidence（字段置信度）
   - 操作：选择图片/拍照/裁剪 → 上传（提示）→ 云端识别 → 预览/校正 → 确认导入/跳过/合并冲突

8) 同步/冲突（Sync / Conflict）
   - 必要数据字段：conflictId、recordType（card/course/note）、localVersion、remoteVersion、status（pending/resolved）、createdAt
   - 操作：自动检测冲突、弹出保留双方提示、进入冲突中心处理

9) 主题/设置（Theme / Settings）
   - 必要数据字段：selectedThemeId（与预设主题映射）、preferences（如 OCR 语言）
   - 操作：切换主题（即时生效并同步）、设置 OCR 语言、删除账号、手动触发同步/导出数据

---

## 6. 首页、课程表、个人中心各模块之间的关系
- 首页 ←→ 课程表
  - 课程表中的“课程”可作为卡片创建时的“所属课程”，首页推荐与今日计划可显示课程标签。
  - 课程表导入（OCR）会生成课程项并自动创建/更新对应的课程集合，供首页展示与筛选使用。

- 首页 ←→ 收藏/集合
  - 首页的推荐卡片可直接收藏到集合或加入今日计划；收藏改变会反映在收藏/集合模块中。
  - 收藏/集合内的卡片出现在首页搜索结果和推荐交互中。

- 首页 ←→ 我（个人中心 / 设置）
  - 主题、偏好（OCR 语言、隐私）影响首页展示（主题配色、是否显示推荐理由等）。
  - 用户可在个人中心查看同步状态、触发强制登出其它设备或删除账号，影响所有模块的数据访问和同步行为。

- 课程表 ←→ 收藏/集合
  - 课程可作为集合入口，课程下可查看与该课程关联的所有知识卡片；集合也可跨课程包含卡片。

- 我（设置） ←→ 同步 / 冲突
  - 设置页提供隐私与同步说明、手动备份/导出/删除云数据、查看冲突中心入口。

---

## 7. 本地数据文件、备份和恢复要求
目标：保证数据在刷新/关闭/重启后不丢失，支持用户导出/导入备份与恢复冲突时的数据安全。

本地持久化要求：
- 所有用户数据（用户资料、卡片、笔记、课程、集合、计划、待上传附件元信息）必须在本地持久化（离线可用），即使应用关闭或崩溃后也能恢复。
- 推荐使用 Firestore 的本地缓存 + 本地持久化方案（或补充本地数据库如 Hive/SQLite）以保存未同步的变更与附件元数据。

备份与导出/导入：
- 必须提供“导出数据”功能，导出内容至少包含：用户卡片、笔记、课程、集合、计划与资源元信息（JSON 格式）。附件文件可作为单独 ZIP 包导出或以指向 Cloud Storage 的链接导出（由用户选择）。
- 必须提供“导入数据”功能，支持导入同格式的 JSON（并在导入前提示字段映射与可能的冲突）。
- 导出与导入应可在设置页手动触发；导入时若与当前数据发生冲突，按冲突策略处理（v1：保留双方并提示用户选择）。

云端备份与恢复：
- 用户数据默认同步到 Firebase Firestore 与 Cloud Storage（附件）。当用户删除账号，应提供清除云端数据的选项（并在删除时明确提示将无法恢复）。
- 在用户首次登录或切换设备时，系统应优先从云端拉取数据并与本地合并（离线优先策略），并在合并时处理冲突。

恢复策略与冲突注意：
- 恢复备份或从云端同步时，如果出现重复项或字段冲突，初版策略为“保留双方并提示用户在冲突中心合并或删除重复项”。
- 附件在本地导入时应校验完整性（例如文件 hash）；若附件文件丢失，保留元数据并提示用户重新上传。

安全与隐私提示（备份相关）：
- 在导出/上传备份前提醒用户所含内容与隐私风险（比如 OCR 上传图片包含敏感信息）。
- 云端不做端到端加密（服务器端默认存储），但导出文件建议用户自担安全或提供加密压缩选项（后期考虑）。

---

## 8. 第一版必须完成的功能（v1 MVP 清单）
以下为必须实现并可验收的功能（优先级高）：

账号与鉴权
- 邮箱 + 密码注册/登录，邮箱验证
- 手机号 + 验证码登录（短信）
- 找回/重置密码
- 单设备登录控制与强制登出其它设备
- 用户资料编辑（用户名、头像、邮箱）
- 删除账号（并支持清除云端数据）

数据存储与同步
- 使用 Firebase（Auth、Firestore、Cloud Storage）
- 本地持久化（离线编辑）并在联网时自动同步
- 冲突检测并保留双方，提示用户选择（冲突中心）
- 附件上传到 Cloud Storage（默认行为）

知识卡片与笔记
- 卡片 CRUD（标题、描述、课程、标签、优先级、来源、关联资源、分类）
- 卡片详情页：显示元数据、关联资源、笔记区
- 笔记 CRUD（纯文本、图片、手写/拍照、附件 PDF/文件）
- 收藏/组织：集合/专题板（卡片可属于多个集合）
- 批量操作：批量移动、批量删除、基本导出（JSON 或 文本）

课表与 OCR
- 手动创建/编辑课程（课程名、教师、地点、周次、星期、节次/时间、学期、备注）
- 支持多个学期并能切换
- 从图片/截屏导入课表：默认上传云端识别（简体中文 + 英文 + 数字），识别结果在导入预览页显示并允许人工校正后导入
- 导入冲突询问（合并/覆盖/跳过），导入前预览和字段映射

搜索与发现
- 首页顶部搜索（按卡片标题，模糊匹配 + 自动补全）
- 每日推荐（每次打开随机刷新）并支持手动刷新与无限滚动
- 推荐卡片可加入计划、收藏、分享、固定

每日计划
- 手动创建计划（以时间段/预计时长为单位）
- 拖拽排序、标记完成、延后/跳过
- 首页显示今日计划（向下滑动加载更多）

主题与设置
- 预设主题（浅色/深色/高对比度等），手动切换并即时生效
- 主题随账号云端同步并在本地持久保存
- 隐私页说明 OCR 上传与数据同步内容
- 导出/导入数据（基础 JSON 导出/导入功能）

产品质量保障
- 数据在刷新/关闭/重启后不丢失（本地持久 + 云同步）
- OCR 上传前提示并需用户确认
- 所有关键操作有成功/失败提示与撤销（短时）机制

---

## 9. 第一版暂时不做的功能（可在后续版本规划）
- iOS 版本（仅 Android 首发）
- 完整的 SRS（间隔重复）学习系统（后期考虑）
- 高级推荐算法（云端 ML / 协同过滤）
- 推送通知（开始提醒/未完成提醒/学习总结）
- 端到端加密（使服务端不可见用户数据）
- 高级 OCR 图像预处理（自动去透视/去模糊等复杂处理）
- 与 Google Calendar / 校园教务 API 的自动同步（仅支持手动导入或 OCR）
- 主题导入/分享（v1 仅提供我们预设主题供选择）
- 附件配额/付费存储模型与 CDN 优化（后续）
- 完整笔记版本历史与逐字段回滚（后期考虑）
- 复杂的在线协作功能（多人同时编辑卡片）（后期）

---

## 10. 可以实际检查的验收标准（可执行测试项）
（略：见仓库 PRD 原文）

---

